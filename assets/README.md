# Assets

This folder stores game art and media files.

Current placeholders:
- `textures/grid.svg`: tiled texture used for the world floor.

You can extend this with:
- GLB car models in `assets/models/`
- obstacle textures in `assets/textures/`
- audio in `assets/audio/`
